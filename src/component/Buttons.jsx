import React from 'react'

const Buttons = (props) => {
  return (
    <div>
        <button style={{height:"50px",width:"70px"}}>{props.title}</button>
      
    </div>
  )
}

export default Buttons
